# dfpp.io — Cloudflare Runbook

Migrated from Netlify (site `dfpp-io`, id `266ca9ab-e2d9-4059-a3a4-a29c48394ad4`) to Cloudflare Workers. Full migration record: `dfpp-infra-migration/migrations/dfpp-agency/slot-02-dfpp-io/` (DISCOVERY.md, PLAN.md, VERIFICATION.md).

## Architecture

- **Worker**: `dfpp-io` — static assets (`dist/`, generated by `scripts/build.mjs` since there's no bundler) with SPA fallback, plus a custom `fetch` handler for form ingestion. Entry: `worker/index.js`.
- **No database.** Static single-page brand hub.
- **R2 bucket**: `dfpp-io-submissions` — durable JSON archive of every contact-form submission.
- **Secrets**: Doppler project `dfpp-io` (config `prd`) → pushed to the Worker via `scripts/sync-secrets-to-worker.sh`. Names: `TURNSTILE_SECRET_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (both Resend values reused by-value from the `dfppagency-web` Doppler project — Resend keys aren't per-domain), `NOTIFICATION_EMAIL`.

## Build & deploy

```bash
npm run build     # scripts/build.mjs -- copies index.html/src/public/_headers into dist/
npm run deploy    # build + wrangler deploy
```

No bundler/framework on this site by design (see `CLAUDE.md`) — `build.mjs` exists purely so the Wrangler assets directory never accidentally includes repo internals (`.git`, `.claude`, `docs/`, `CLAUDE.md`, `netlify.toml`, `worker/` source, `node_modules`). `dist/` is gitignored, regenerated every build.

### Turnstile (replaces Netlify's reCAPTCHA integration)

Netlify's `data-netlify-recaptcha` was 100% platform magic — the actual reCAPTCHA widget/script/sitekey was injected into the HTML at Netlify's build time, invisible to the repo, with server-side validation baked into Netlify Forms. Confirmed via baseline testing that Netlify rejects (400) *any* submission lacking a valid token, independent of the honeypot — that's the invariant preserved here.

- `index.html`'s contact form has a real `<div class="cf-turnstile" data-sitekey="...">` and loads `https://challenges.cloudflare.com/turnstile/v0/api.js`. The widget auto-populates a `cf-turnstile-response` field that rides along in the existing `FormData` — no change needed to `src/main.js`'s submit handler.
- `worker/turnstile.js` verifies that token against `https://challenges.cloudflare.com/turnstile/v0/siteverify` using `TURNSTILE_SECRET_KEY`.
- `worker/forms.js` checks the honeypot (`bot-field`) **first** — cheap, fails fast for bots without spending a Turnstile API call — then Turnstile. Missing/invalid token → 400.
- **Currently deployed with Cloudflare's official "always passes" TEST key pair** (site key `1x00000000000000000000AA`, secret `1x0000000000000000000000000000000AA` — note: the correct secret has 33 zeros between `1x` and `AA`; an earlier attempt with 30 zeros failed with `invalid-input-secret`). **Must be swapped for a real widget's keys before cutover** — see "Before cutover" below.

### Form ingestion (replaces Netlify Forms)

- Endpoint stayed at `POST /__forms.html` — the existing `fetch()` call in `src/main.js` needed no changes.
- `worker/index.js` intercepts that one route; everything else falls through to `env.ASSETS`.
- `worker/forms.js`: honeypot → Turnstile → (if both pass) archive to R2, send operator notification, send submitter confirmation email. Fails open on email/archive errors (logged, never changes the 200 response).
- **Enhancement over the old site**: a confirmation email now goes to the submitter (`worker/emails.js` `sendContactConfirmation`) — the old Netlify site only notified the operator via its dashboard, with no submitter-facing email at all. Approved in `slot-02-dfpp-io/PLAN.md`.

### Headers

`_headers` at repo root (copied to `dist/_headers` by the build script) — `X-Frame-Options: DENY`, `Cache-Control: public, max-age=3600`, and an explicit `Strict-Transport-Security` header (Netlify added HSTS as a platform default that does NOT carry over automatically — same lesson from the main site migration). Dropped the deprecated `X-XSS-Protection` header (modern browsers ignore it).

## Secrets (operator-run, outside Claude Code)

1. Doppler project `dfpp-io` (config `prd`) — already exists.
2. Populate `RESEND_API_KEY` / `RESEND_FROM_EMAIL` (copy the values from `dfppagency-web`'s Doppler config), `NOTIFICATION_EMAIL`, and `TURNSTILE_SECRET_KEY`.
3. `bash scripts/sync-secrets-to-worker.sh` — pushes them to the Worker via `wrangler secret bulk`.

## Before cutover — swap the Turnstile test key for the real one

1. Cloudflare dashboard → Turnstile → Add widget → hostname `dfpp.io`. Copy the site key and secret key.
2. Edit `index.html`: replace `data-sitekey="1x00000000000000000000AA"` with the real site key (remove the TODO comment above it).
3. `doppler secrets set TURNSTILE_SECRET_KEY "<real secret>" --project dfpp-io --config prd`, then re-run `scripts/sync-secrets-to-worker.sh`.
4. `npm run deploy`.
5. Manually submit the live contact form once (a real browser, solving the actual widget) to confirm the real key pair works before DNS cutover.

## CI/CD (Workers Builds) — connected and confirmed working

Unlike the main site, this repo has no build step involving a headless browser — **Workers Builds works cleanly here**, confirmed end to end: connected, root directory `/` (empty/blank in the dashboard field since `package.json` lives at repo root, not a subdirectory), build `npm ci && npm run build`, deploy `npx wrangler deploy`. First push to `main` after connecting produced a clean `stopped/success` build in ~25 seconds. No build-time public vars needed.

Going forward: **push to `main` and Workers Builds deploys automatically.** Manual `npm run deploy` remains available as a fallback but isn't the standard path here (unlike the main site, where it is).

## Manual deploy fallback

`npm run deploy` — no special environment requirements (no Chromium needed, unlike the main site).

## Rollback

DNS/Custom-Domain removal reverts to Netlify (kept live-idle 48–72h+ after cutover). No data migration (no DB); the 4 existing Netlify form submissions are unaffected either way.

## Known deviations from the original Netlify setup

- `__forms.html` (Netlify's build-time form-detection stub) was deleted — no function once the Worker handles `/__forms.html` directly.
- Confirmation email to the form submitter is new (see above) — not present on the old site.
- `X-XSS-Protection` header dropped (deprecated, no-op in modern browsers).
